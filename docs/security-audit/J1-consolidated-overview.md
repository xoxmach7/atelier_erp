# J1 — Consolidated Security Architecture Overview

Дата: 2026-07-20. Синтез этапов B1–I1 ([полный список](../SECURITY_ARCHITECTURE_AUDIT.md)). A1 (scope) остаётся ручной задачей владельца — не блокировал остальные этапы, т.к. масштаб системы был очевиден из кода.

> **Обновление 2026-07-20 (тот же день):** из блока «Сделать сейчас» закрыты #1 (общий throttle-кеш), #2 (`ensure_superuser` больше не откатывает вручную изменённый пароль), #5 (логирование неудачных логинов), #7 (простой CI). Детали — в CLAUDE.md. Остались нетронутыми: #3 (`DATABASE_PUBLIC_URL`), #4 (JWT в `localStorage` на вебе), #6 (бэкапы) — требуют решения владельца или доступа к консолям Railway/Cloudflare, не только код.
>
> **Обновление 2026-07-25:** закрыты #8 (Owner больше не получает все Django permissions — убрано из `seed_groups.py` + data-миграция `0034` чистит уже накопленные в БД), #9 (throttle 5/мин на `/admin/login/` и `/api/auth/login/` — `atelier_erp/login_throttle.py`, до этого только JWT-логин `/api/auth/token/` был защищён), #10 (`JWT_SIGNING_KEY` отделён от `DJANGO_SECRET_KEY`, откатывается на него если не задан в окружении — рекомендуется задать отдельное значение при следующей ротации), #11 (регламент ротации — [SECRET_ROTATION_POLICY.md](SECRET_ROTATION_POLICY.md)), #12 (кастомный `EXCEPTION_HANDLER` логирует все 401/403 с path/method/user/ip, не только логин), #15 (`DB_SSLMODE` по умолчанию `'require'` вне `DEBUG`, `'prefer'` в dev — переменная окружения `DB_SSLMODE` по-прежнему может переопределить явно; **не проверено на реальном Railway** — если managed Postgres Railway не поддерживает `require` на используемом пути соединения, это может сломать доступ к БД, стоит проверить при следующем деплое). Тесты: `test_security_audit_2026_07_25.py` (11). Backend 292.
>
> **Также в этом же заходе закрыт отдельный, более серьёзный баг, не входивший в исходный список 25 находок**: межателье-изоляция через API не работала вообще ни для одного запроса (`TenantMiddleware` резолвил тенант до JWT-аутентификации DRF) — прямое воплощение риска #16 «RBAC/tenant-скоуп держится на дисциплине, а не на декларативной защите», только на уровень серьёзнее предполагавшегося. См. `atelier_erp/tenant_drf.py` и память проекта `project_tenant_isolation.md`.
>
> **Всё ещё открыто**: #3 (`DATABASE_PUBLIC_URL`), #4 (JWT в `localStorage` на вебе — сознательно не трогали в этом заходе: нетривиальная переделка auth end-to-end, затрагивает и веб, и мобилку, и требует отдельного дизайн-обсуждения, а не батча точечных фиксов), #6 (бэкапы), #13/#14/#17 (инфраструктурные/организационные, не код).

## 1. Целевая архитектура безопасности (кратко)

Система — монолит на управляемых платформах (Railway + Vercel + Cloudflare R2), без собственной сети/файрвола. Правильная модель угроз для такой системы — не «защита периметра», а:

1. **Единая точка входа (`/api/v1/`) с надёжной аутентификацией/авторизацией** — RBAC уже есть и в целом работает, но держится на дисциплине разработчика (`get_queryset()` в каждом ViewSet), а не на декларативной второй линии защиты.
2. **Секреты и учётки — единственный настоящий периметр.** При отсутствии сетевой изоляции всё сводится к тому, кто знает пароль/ключ. Отсюда: MFA для superuser, ротация секретов, разделение `DJANGO_SECRET_KEY`/JWT-ключа, закрытие `DATABASE_PUBLIC_URL`.
3. **Видимость (логи/аудит) как компенсация отсутствия сетевого мониторинга** — раз нет файрвола/IDS на сети, обнаружение атаки должно происходить на уровне приложения (логин-попытки, 403-отказы), чего сейчас почти нет.
4. **Automated security gate в CI** — при команде из одного человека это единственная защита от «забыл проверить перед деплоем».
5. **Проверенная возможность восстановления** — единственная защита от необратимой потери данных при отсутствии инфраструктурной избыточности.

## 2. Сводный список находок (по приоритету)

### High

| # | Находка | Этап | Что делать |
|---|---|---|---|
| 1 | Throttle-кеш не общий между воркерами (`LocMemCache`, нет `CACHES` в `settings.py`) — брутфорс-защита логина слабее заявленной (5/мин → фактически ~15/мин, сброс при рестарте) | B2 | Настроить общий кеш (файловый или БД-кеш достаточно, Redis не обязателен) |
| 2 | Единственный superuser, пароль пересоздаётся на каждом деплое (`ensure_superuser`), нет MFA | B2, C1, F1 | Убрать автосброс пароля из `CMD` (или осознанно принять и задокументировать), добавить MFA |
| 3 | `DATABASE_PUBLIC_URL` — постоянный публичный вход в БД в обход всего приложения | B2, D1 | Проверить, можно ли отключить публичный прокси Railway; если нет — минимизировать время жизни известного пароля |
| 4 | JWT (access + refresh) в `localStorage` на вебе — при любом будущем XSS даёт захват аккаунта на 7 дней, не на 60 минут | E1 | Перевести хранение (минимум refresh) на httpOnly-cookie |
| 5 | Нет логирования неудачных попыток логина — невозможно расследовать брутфорс постфактум | G1 | Добавить `logger.warning` в `ThrottledTokenObtainPairView` |
| 6 | Нет подтверждённой стратегии бэкапов БД, ни разу не тестировалось восстановление | H1 | Проверить/включить автобэкапы в Railway-консоли, один раз протестировать restore |
| 7 | Нет вообще CI/CD security-гейта (тесты/dependency scan/secret scan не автоматизированы) | I1 | Простой GitHub Actions workflow: тесты + `pip-audit`/`npm audit` + secret-scan |

### Medium

| # | Находка | Этап |
|---|---|---|
| 8 | ✅ **Сделано 2026-07-25.** Группа Owner получает `Permission.objects.all()` в Django admin — латентная эскалация, если кто-то получит `is_staff=True` | C1 |
| 9 | ✅ **Сделано 2026-07-25.** Admin-логин и browsable API DRF не имеют выделенного `login`-throttle | C1 |
| 10 | ✅ **Сделано 2026-07-25.** `DJANGO_SECRET_KEY` одновременно подписывает сессии/CSRF и JWT — компрометация одного даёт оба | F1 |
| 11 | ✅ **Сделано 2026-07-25.** Нет процедуры/расписания ротации ни для одного секрета | F1 |
| 12 | ✅ **Сделано 2026-07-25.** Нет логирования 403/отказов в доступе | G1 |
| 13 | Логи не централизованы, срок хранения на Railway не подтверждён | G1 |
| 14 | Нет staging-окружения, изолированного от прода | I1 |
| 15 | ✅ **Сделано 2026-07-25.** `DB_SSLMODE` по умолчанию `'prefer'`, не `'require'` — не проверено, что жёстче в Railway Variables | E1 |
| 16 | Архитектурный паттерн: RBAC/tenant-скоуп держится на дисциплине `get_queryset()`, а не на второй декларативной линии защиты — корневая причина всех прошлых межтенантных утечек (уже закрытых) | B2 |
| 17 | API и Django admin на одном публичном домене без сетевого разделения (компенсируется только логином) | D1 |

### Low

| # | Находка | Этап |
|---|---|---|
| 18 | История статусов/аудит-логи не защищены от прямого изменения в БД | B2 |
| 19 | Browsable DRF API включён в проде без явного решения | B2 |
| 20 | Encryption-at-rest для Postgres/R2 не подтверждён документацией провайдера | E1 |
| 21 | Предсказуемость путей файлов в R2 не проверялась | B2, E1 |
| 22 | Нет egress-allowlist на уровне кода/сети (защита от SSRF — только ревью) | D1 |
| 23 | Django `admin.LogEntry` существует, но не используется как рабочий инструмент | G1 |
| 24 | Пароли пилотных пользователей проходят через stdout команды (`seed_pilot`) | F1 |
| 25 | Single points of failure на каждом уровне (1 backend, 1 БД, 1 bucket) — ожидаемо для масштаба 1 клиента | H1 |

## 3. Roadmap

**Сделать сейчас (до второго клиента), дёшево и весомо:**
- #5 (логирование неудачных логинов) — несколько строк кода.
- #1 (общий кеш для throttle) — одна настройка `CACHES`.
- #6 (проверить/включить бэкапы Railway + тест restore) — не код, но критично и дёшево.
- #7 (простой CI-воркфлоу с уже существующими зелёными тестами) — тесты уже написаны, не хватает только автозапуска.
- #2 (решить судьбу `ensure_superuser`-автосброса пароля) — однострочное решение (убрать вызов из `CMD` или явно задокументировать и дать MFA).

**Сделано 2026-07-25:** #8, #9, #10, #11, #12, #15 — см. обновление в шапке документа.

**Осталось на следующие спринты:**
- #4 (JWT-хранение на вебе) — требует изменений на backend (выдача httpOnly-cookie) и фронте, нетривиально, но самый весомый пункт из «данных». Сознательно не тронут 2026-07-25 — нужен отдельный дизайн (веб vs мобилка используют разный способ хранения токена, менять на ходу рискованно для единственного реального пилота).
- #3 (`DATABASE_PUBLIC_URL`) — зависит от возможностей плана Railway, нужно сначала выяснить, что вообще доступно.
- #6 (бэкапы Railway + тест restore) — не код, консоль Railway.

**Наблюдать / переоценить при росте (после 2–3 клиентов):**
- #14 (staging-окружение) — оправдано, когда цена ошибки в проде вырастет.
- #16 (архитектурный паттерн RBAC) — не переписывать превентивно, но держать в голове при добавлении каждого нового ViewSet; можно рассмотреть общий миксин/декоратор, который проверяет, что `scope_to_tenant()`/`scope_orders_for_role()` вызван, если появится третий похожий инцидент.
- #17 (разделение admin от публичного домена) — не first-class задача на PaaS без второго origin, переоценить при найме второго разработчика.
- #25 (SPOF на каждом уровне) — станет реальной проблемой только при заметном росте нагрузки/числа клиентов.

## 4. Допущения и остаточные риски

- **A1 (scope) не был формализован отдельным документом** — аудит охватил весь код и все окружения (dev/prod), но не проходил через явное подтверждение владельца, что именно критично для бизнеса и есть ли регуляторные требования (GDPR и т.п. — Sheber работает с ПДн клиентов ателье в Казахстане; применимость конкретных норм не оценивалась).
- **Backup/DR (H1) и организационные вопросы доступа (F1 §5) — за пределами того, что видно из кода.** Требуют прямой проверки в консолях Railway/Vercel/Cloudflare, не в репозитории.
- **Аудит не включал реального penetration-теста** (SQLi/XSS/auth-bypass через фактическую эксплуатацию) — все находки основаны на чтении кода/конфигурации, не на попытках взлома живой системы. Для системы с реальными платежами клиентов стоит рассмотреть внешний pentest при росте масштаба.
- **DEBUG/секреты/значения переменных в реальном Railway/Vercel окружении не читались** — аудит намеренно ограничился структурой кода, не содержимым `.env` в проде.
- Все находки уровня High/Medium не являются подтверждёнными эксплуатируемыми уязвимостями «уже произошедшими» — это архитектурные риски и пробелы в защите, приоритизированные по вероятному ущербу при реализации.
