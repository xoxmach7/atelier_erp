# Memory — Atelier ERP / Sheber

## Правила работы
- **НЕ коммитить без явного одобрения пользователя.** Всегда показывать `git diff --stat` и ждать подтверждения перед `git commit`.
- **CLAUDE.md должен быть всегда синхронизирован с реальным состоянием репозитория.** После значимых коммитов (security-фиксы, архитектурные изменения, закрытие задач трекера) — обновлять этот файл в том же заходе, не откладывать. Раз в несколько сессий сверять трекер с `git log` на расхождения.
- **Когда нужна спека (brainstorming) перед кодом, а когда можно сразу:**
  - Спека нужна, если: есть развилка (больше одного разумного способа сделать), задача затрагивает несколько файлов/систем сразу, цена ошибки высокая (бизнес-логика, данные пользователей), или запрос сформулирован как цель, а не как решение ("улучшить структуру", "сделать по уму").
  - Спеку можно пропустить и делать сразу, если: задача однозначна и локальна (один файл, один явный фикс, нет развилки), это точное повторение уже одобренного паттерна, или это чисто механическая правка без дизайн-решений.
  - Критерий: если после прочтения задачи есть один явный путь реализации — код сразу. Если есть развилка или формулировка описывает цель, а не решение — сначала спека (`docs/superpowers/specs/`), затем план (`docs/superpowers/plans/`).

## Проект
**Sheber ERP** (он же Atelier ERP) — ERP для шторного ателье.
Репо: https://github.com/xoxmach7/atelier_erp.git
Ветка в работе: `main` (fix/p0-access-security смёрджена)
Теги: `v0.1-cleanup` (до P4), `v0.2-pre-multi-tenancy` (до мёржа multi-tenancy 2026-06-13)

## Стек
| Слой | Технология |
|------|-----------|
| Backend | Django 4.2 + DRF + PostgreSQL |
| Frontend | Next.js (TypeScript, Tailwind, shadcn) |
| Mobile | React Native + Expo + TypeScript |
| Deploy | Railway (Gunicorn + WhiteNoise) |
| Auth | JWT (SimpleJWT) + RBAC (Django Groups) |

## Роли (канонические)
| Роль | Что видит |
|------|-----------|
| **Owner** | Всё: заказы, деньги, сотрудники, аналитика |
| **Designer** | Все заказы, замеры, КП |
| **Warehouse** | Только задачи по материалам (in_work/in_production/ready) |
| **Seamstress** | Только in_production |
| **Installer** | ready / on_installation / waiting_final_payment |

## Термины проекта
| Термин | Значение |
|--------|---------|
| КП | Коммерческое предложение (расчёт стоимости) |
| P0 | Блок безопасности и доступа (выполнен) |
| P1 | Блок надёжности данных (выполнен) |
| P2 | Архитектурные улучшения (выполнен) |
| P3 | Низкий приоритет / косметика (выполнен) |
| P4 | Инфраструктура SaaS (в работе) |
| P5 | Масштаб 5+ клиентов (планируется) |
| settings_test.py | Test-settings с SQLite для CI без PostgreSQL |
| seed_groups | `manage.py seed_groups` — создаёт канонические роли в БД |
| seed_pilot | `manage.py seed_pilot` — тестовые аккаунты по ролям |

## Статус задач (трекер)

### ✅ Закрытые блоки
- **P0** Безопасность и доступ — done
- **P1** Надёжность данных — done
- **P2** Архитектурные улучшения — done
- **P3** Косметика / чистка — done
- **P4-CRITI** S3/R2 хранилище медиафайлов — done 2026-06-09
- **P4-SEC** Throttling 200/min user, 20/min anon — done 2026-06-09
- **P4-CFG** CORS без LAN-IP — done 2026-06-09
- **P4-ARCH** Multi-tenancy — done, несколько заходов:
  - 2026-06-13 (35d3149): базовая модель Tenant, TenantMembership, middleware, migrations 0018+0019.
  - 2026-07-06 (50edd15): `Task` (лиды) оставался без изоляции — добавлено поле `tenant` (migration 0022), `TaskViewSet` → `TenantModelMixin`.
  - 2026-07-06/07 (419533f, 866025c, 884ee0c, df4cb81, 784e63c, 46385a6): второй, более глубокий заход — `TenantManager` на уровне ORM с `ContextVar` для текущего tenant (дефолт `ALL_TENANTS` вне HTTP-контекста), раскатан на `Order`, `Task`, `InventoryItem`, `ProductionAssignment`, `SeamstressPayment`. Задокументирована двухуровневая защита (manager + DRF mixin) и предупреждение, что для Payment/Quote/Measurement работает только одна линия защиты (144897e, 958bc3f, 72961b2).
  - 2026-07-07 (68e7aa5): `Fabric` (каталог тканей) изолирован по tenant — закрыта ещё одна межтенантная утечка склада.
- **Security** (419533f, 71c6e9a): проверка загружаемых файлов по magic bytes (не по расширению), обновлён xhtml2pdf (CVE-2024-25885), fail-fast если `DJANGO_SECRET_KEY` не задан в проде.
- **Frontend** редизайн v2, orders/[id] v4, workspace, customers, work-экраны — done
- **B1** responsible_user + designer select + лейбл — done 2026-06-13
- **Склад** общий инвентарь `InventoryItem` (категория/единица/кол-во/цена/порог «на исходе»), API `/api/v1/inventory-items/` (чтение всем, запись склад/владелец, soft-delete), миграция 0021. Экран «Материалы»: объединённая таблица (ткань Fabric + позиции InventoryItem) + добавление/редактирование/удаление. `Fabric` оставлен как каталог КП. — done 2026-06-21 (3bb8840)
- **Fix** dashboard: редирект по роли вынесен в useEffect (был setState-in-render) — done 2026-06-21 (47c7fa6)
- **Mobile (владелец+дизайнер) под Figma** — done 2026-06-21 (774561b). Экраны: дашборд владельца (today.tsx: период+сегмент Прибыль/Выручка/Расходы, bar-chart с осью/сеткой, плитки-статы), Управление заказами (orders.tsx), деталь Заказ №N ([id].tsx: адрес+замеры+«Создать КП»), создание/редактирование заказа (OrderForm), клиенты (clients/index|new|[id]), замер (MeasurementForm), КП (quote.tsx). Инфра: компонент `src/components/Icon.tsx` (иконки на View/Text, без либ), API `customers`/`staff`/`fabrics`, `del` в client, `deleteOrder`. tsc mobile = 0.
- **M1: Mobile роли Пошив/Установка** — done 2026-07 (db51b40 и далее). Экраны work.tsx под Figma (таблицы как у склада), фильтр статуса заказов на дашборде, второй экран КП (Установка/Скидка/ИТОГО/Предоплата/Скачать КП — 7606305), экран логина приведён к Figma (fda53bb), иконки переписаны на react-native-svg (7d8486a), кнопка загрузки фото монтажника подключена (cc4d404). Плюс тестовая инфраструктура mobile: jest-expo + @testing-library/react-native, покрытие ApiClient (retry на 401, дедупликация refresh) и API-модулей orders/customers/fabrics/payments/staff/work.
  - **Follow-up мобилки (M2)**: (1) замер: тюль пишется в comment — нужно расширить бэк-эндпоинт замера на curtain+tulle (MeasurementWriteSerializer вместо MeasurementCreateSerializer в POST /orders/{id}/measurements/); (2) КП: цены вводятся вручную (автарасчёт = запаркованный КП-калькулятор); (3) «Выбрать период» на дашборде — заглушка. Мобилка не «деплоится» — гоняется через Expo/EAS.
- **Mobile design polish** (2026-07-11, 91932d9): `work.tsx` использовал локальный emoji/text `IconButton` (⌕/≡) вместо общего SVG icon-набора — заменён на `IconButton` из `Icon.tsx`, добавлена иконка `menu`. `OrderForm`: поля "Дата замера"/"Завершение" были текстовым вводом `__.__.____` без валидации — заменены на `@react-native-community/datetimepicker` (Android: системный modal, iOS: spinner в Modal с кнопкой «Готово»). tsc чистый, mobile suite 55/55. Не проверено визуально в симуляторе (в песочнице нет Android/iOS эмулятора) — рекомендовано пользователю прогнать через Expo Go перед мержем. Проверка показала, что старый нередизайненный `work.tsx` из допилотной эпохи (упомянутый ранее в этом файле) уже был удалён раньше — эта заметка была устаревшей.
- **Landing page** (landing.html, корень репо) — редизайн под ui-ux-pro-max design system (палитра B2B Service `#0369A1`/`#0F172A`, шрифт Plus Jakarta Sans, секция сравнения «без системы vs Sheber Atelier», a11y-доработки). Ребрендинг Sheber ERP → **Sheber Atelier** на лендинге. Задеплоено отдельным Vercel-проектом: https://sheber-atelier-landing.vercel.app (аккаунт bohemetextile8-2199, без git-интеграции — редеплой руками через `vercel --prod` из копии файлов). Логотип: `media/newlogo.webp` (добавлен в git через `git add -f`, т.к. `media/` в целом в `.gitignore`).
- **Security-аудит backend** (2026-07-11, 92c0292): найдена и закрыта межтенантная утечка платежей в `FinanceWorkQueueView` (запрашивал `Payment.objects` без tenant-фильтра — любой Owner/Manager видел последние 20 платежей всех ателье; добавлен `TenantViaOrderMixin` + `scope_to_tenant()`). Закрыта HTML-инъекция в KP PDF (`quote_service.generate_pdf` вставлял поля клиента/комнаты без экранирования — теперь `html.escape()`); `_link_callback` xhtml2pdf больше не резолвит произвольные URI (только локальные шрифты) — закрыт потенциальный SSRF. Backend suite 79/79 зелёные после фикса.
  - **Отклонено при верификации, не требует фикса**: Quote IDOR (копирование чужого КП через `generate_items_from_quote`, `views.py:~890`) — `Quote.id` UUID, неугадываем по конвенции проекта, но tenant-проверка там всё же отсутствует как defense-in-depth (не критично, можно доделать при следующем заходе на Quote-flow).
- **Dependency audit** (2026-07-11, 6c19688): backend `pip-audit` 8→0 уязвимостей (Pillow 11.3.0→12.3.0 — закрыт CVE-2026-42311 memory corruption через PSD, эксплуатируемо через загрузку фото замеров/установки, вызывающую `Image.open()`; pytest 8.4.2→9.1.1). Frontend `npm audit` 7→4 (vitest 2.1.9→4.1.10 — закрыт critical CVSS 9.8 произвольное чтение/исполнение файлов через vitest UI-сервер; остаток — postcss внутри next, ложный совет резолвера откатить next до 9.3.3, игнорируем). Mobile `npm audit` 17→14 (undici HIGH + tar + js-yaml закрыты non-breaking; остаток требует мажорного Expo 54→57, не делали). Тесты после апгрейда все зелёные (backend 119/79, frontend 23, mobile 55).
- **Client-side + инфра security review** (2026-07-11, c13d144): frontend/mobile чисты (нет XSS/`dangerouslySetInnerHTML`/`eval`/open-redirect/отключённого TLS). Django settings (DEBUG/ALLOWED_HOSTS/CORS/CSRF/SECRET_KEY) все корректны. Исправлено: Dockerfile запускал контейнер от root (нет `USER`) — добавлен непривилегированный пользователь `app`, `chown -R` на `/app`, `USER app` перед `CMD`; проверено live-сборкой (`whoami`/`id` внутри контейнера → `app` uid 999, статика читается/пишется).
- **Quote IDOR defense-in-depth + mobile SecureStore** (2026-07-11, d37db00): `_get_source_quote` (`order_item_generation_service.py`) теперь проверяет `quote.order.tenant_id == order.tenant_id` для явно переданного `quote_id` — закрывает теоретическую дыру, где чужой approved-КП можно скопировать в свой заказ, если UUID где-то засветился. Mobile: JWT-токены (access/refresh) перенесены из `AsyncStorage` (незашифровано) в `expo-secure-store` (Keychain/Keystore); `atelier_user` (не секрет) остался в AsyncStorage. Добавлен jest-мок `mobile/__mocks__/expo-secure-store.ts`. Тесты: backend 119/119, mobile 55/55, tsc чистый.

- **M2.2 + M2.1: Авторасчёт метража + тюль в свои поля** (2026-07-11): метраж ткани/тюля больше не вводится руками — вычисляется сервером из ширины окна и **настраиваемого на окно** коэффициента сборки (раздельные `curtain_gathering` 2.2 / `tulle_gathering` 2.0 на модели `Measurement`, migration 0024). Формула по модели «сборка»: `meters = ceil₀.₁(width_cm × gathering / 100)`, вынесена в `atelier_erp/services/measurement_calc.py` (единая точка изменения округления). Write-path — action `POST /orders/{id}/measurements/` + переработанный `MeasurementCreateSerializer` (обе ткани по имени + коэффициенты; метраж read-only, присланный клиентом игнорируется). Закрыт M2.1: тюль (ткань+коэффициент) уходит в свои поля, а не в `notes`. Мобилка: `MeasurementForm` — 2 выпадашки сборки + живая подсказка «≈ X.X м»; легаси-экран `orders/[id]/measurements.tsx` мигрирован синхронно (иначе молча ломался бы). Цена КП осталась ручной (по решению пользователя — тарифы `SewingRates`/`GatheringRatios` не задействованы). Web-путь `/api/v1/measurements/` не мигрирован (шлёт метраж вручную, вне скоупа). Тесты: backend 87 (+3 meterage endpoint/формула), mobile 58 (+3 previewMeters); tsc чистый. **Не проверено визуально в симуляторе** (нет эмулятора в песочнице) — прогнать через Expo Go перед мержем. Спека: `docs/superpowers/specs/2026-07-11-quote-meterage-autocalc-design.md`.

- **Пилотные баги с телефона** (2026-07-13): (Bug#4/#3) «Скачать КП»/сохранение КП падало `select_for_update cannot be used outside of a transaction` — `QuoteService.update_quote` обёрнут в `transaction.atomic()` (вызывается из ViewSet без внешнего UnitOfWork; sibling-мутаторы approve/send/reject/revise имели тот же латентный баг — обёрнуты в atomic превентивно в аудите 2026-07-15, хотя из API не вызываются). «Перекидывает на предоплату» после создания КП — это штатный 2-й экран КП, не баг. (Bug#1/#2) экран деталей заказа не показывал замеры/дату замера/адрес: `OrderExecutionService.get_order_execution_summary` не отдавал их на верхнем уровне (замеры жили только в `role_sections.designer`, а адрес мобилка кладёт в `order.installation_address_*`, тогда как экран читал `customer.address`). Добавлены top-level `measurement_date`/`installation_date`/`planned_completion`/`installation_address`/`measurements`; мобильный `[id].tsx` читает `installation_address`. (Bug#5) фильтр-пилюли на экране заказов рендерились обрезанными — горизонтальный `FlatList` с `flexDirection:'row'` в contentContainer заменён на горизонтальный `ScrollView`. Тесты: backend 89 (+2 регрессии), mobile 58, tsc чистый. Мобилка бьёт в Railway prod (`ateliererp-production`), backend-фиксы видны после redeploy.

### 🔲 Активные задачи
| # | Задача | Приоритет |
|---|--------|-----------|
| M2 | Mobile follow-up: авто-расчёт **цены** КП (запаркован, цена пока ручная); «Выбрать период» на дашборде — заглушка; форма замера не умеет «Крепление»/«Фурнитура» (пишет старый `mounting_type`) | по решению |
| 18 | P4-OPS: Email-уведомления (SendGrid/SMTP) | после первого клиента |
| 21 | P5: Автоматический онбординг нового ателье | после 5 клиентов |
| 22 | P5: Биллинг-модель SaaS (Subscription) | после 5 клиентов |

- **Статусы: группы в фильтре + смена статуса с телефона** (2026-07-18): (1) Фильтр-пилюли в мобилке показывали 7 технических статусов FSM — заменены на 4 пользовательские группы (Все / В работе / Просрочен / Завершён / Ожидание). Раскладка группа→статусы задана **на бэке** в новом `atelier_erp/api/v1/filters.py` (`ORDER_STATUS_GROUPS` + `OrderFilterSet.status_group`), чтобы веб и мобилка не разъехались; мобилка шлёт `?status_group=`, `OrderViewSet` перешёл с `filterset_fields` на `filterset_class`. `overdue` — производное состояние (дедлайн прошёл и заказ не в терминальном статусе), считается на сервере, поэтому больше не ломается пагинацией. (2) **Найдена регрессия: мобилка вообще не умела менять статус заказа** — компонент `mobile/src/components/orders/OrderRoleActions.tsx` остался в репо, но нигде не был отрендерен после редизайна `orders/[id].tsx` под Figma. Заказы, созданные с телефона, навсегда висели в `new`. Строка «Статус» в карточке заказа сделана кликабельной: открывает список разрешённых переходов из `data.actions` (правила перехода остаются на бэке, мобилка своей копии FSM не держит) + «Отменить заказ». Дашбордная плитка «Ожидают оплаты» переведена с `?status=waiting_final_payment` на группу `?status=waiting`. Тесты: backend 97 (+5 на группы), mobile 58, tsc чистый, iOS/Android бандлы 200. **Важно про Metro:** в SDK 54 бандл дёргается по `/.expo/.virtual-metro-entry.bundle?platform=...`, старый путь `/expo-router/entry.bundle` отдаёт 404.

- **Автопродвижение статусов** (2026-07-18): статусы двигались только руками, поэтому в системе всегда отставали от реальности. Новый `atelier_erp/services/status_automation.py` с единственной функцией `auto_advance(order, target, reason, changed_by)`. Три свойства: (1) своих правил перехода нет — всё идёт через `OrderService.transition_status_mvp`, те же инварианты, что у ручных кнопок; (2) никогда не роняет основную операцию (запрещённый переход = молчаливый no-op + `logger.info`) — иначе приём платежа падал бы из-за отсутствия АВР; (3) в `OrderStatusHistory.notes` пишется префикс `[авто]`. Триггеры: позиции сформированы из КП → `in_work` (в `OrderItemGenerationService`), КП одобрено при уже готовых позициях → `in_work` (в `QuoteService.approve_quote`), `material_readiness=ready` → `in_production`, `production_stage=done` → `ready`, установка завершена + оплачено → `completed` (раньше считался флаг `can_auto_complete`, но его никто не применял), финальный платёж закрыл остаток → `completed` (только из `waiting_final_payment`, чтобы 100% предоплата не закрывала несшитый заказ). Изначально сознательно НЕ автоматизировался `ready → on_installation` (задумывался как решение человека — назначение бригады), но по факту решения там не было: монтажник и так уже действует по заказу в статусе `ready` (фотоотчёт/АВР/handover разрешены с `ready`, не только с `on_installation`) — шаг владельца был формальностью. **Автоматизирован 2026-07-20** по прямому запросу владельца, см. запись ниже. Переходы в `cancelled` остаются ручными. Спека: `docs/superpowers/specs/2026-07-18-status-automation-design.md`. Тесты: `test_status_automation.py` (12).
- **Веб-фильтр статусов: баг «Просрочено»** (2026-07-18): `frontend/src/lib/list-status.ts` сравнивал `status === "overdue"`, но `overdue` не существует как значение `Order.status` — это производный флаг `is_overdue`. Пилюля «Просрочено» на вебе показывала 0 всегда, а просроченные заказы висели в «В работе». `getListStatus(status, isOverdue)` теперь принимает флаг вторым аргументом (просрочка перебивает стадию, но не у закрытых заказов). Выяснилось, что `is_overdue` вообще не сериализовался в v1 — добавлен в `OrderListSerializer` и `OrderDetailSerializer` + в DTO фронта. Группа `completed` на бэке дополнена `cancelled` (иначе отменённый заказ не попадал ни в одну пилюлю) — теперь совпадает с вебовским ключом `done`.
- **Позиции-услуги в КП** (2026-07-18): `_create_order_item_from_quote_item` жёстко ставил `item_type='fabric'` и `fabric=quote_item.fabric`, поэтому строка КП без ткани (чистая услуга — установка электрокарниза, демонтаж, доставка) роняла генерацию позиций с `IntegrityError: orderitem_valid_reference` (constraint требует ровно одну ссылку под свой `item_type`). Тип теперь выводится из строки КП: ткань → `fabric`, иначе карниз → `cornice`, иначе → `service` (смешанная строка остаётся тканевой, как было). У `QuoteItem` нет FK на `Service` (только `installation_price`/`additional_services_total`), поэтому запись справочника подбирается по `sewing_type` через `get_or_create` с фолбэком «Услуга по КП»; `price_per_unit` у автосозданной записи нулевой — реальная цена лежит на позиции заказа, справочник тут только ярлык. Тесты: `test_order_item_generation_types.py` (6).

- **Экран швеи + права на галочки по окну** (2026-07-18): (1) **Швея и склад упирались в тупик** — `today.tsx` редиректил на список заказов только `designer`, остальные попадали в `NonOwnerView` (очередь работ), где строки `WorkTaskRow` рендерятся без `onPress`. Открыть заказ было невозможно в принципе. Редирект расширен до `ORDER_LIST_ROLES = designer/warehouse/production`. (2) **Складская галочка возвращала 403**: на `MeasurementViewSet` стоял `IsOwnerOrDesigner`. Введён `CanAccessMeasurement` (Owner/Designer — CRUD; Warehouse/Seamstress — только чтение и PATCH) + узкие сериализаторы `MeasurementWarehouseFlagSerializer` / `MeasurementSewingFlagSerializer`, чтобы склад не мог переписать размеры окна или ткань. (3) Новое поле `Measurement.sewing_done` (migration 0026) — швея отмечает пошив по каждому окну; отдельный флаг от `materials_ready`, т.к. роли и стадии разные. (4) **Карточка окна печатала UUID вместо названия ткани**: тип `OrderExecution.measurements` объявлял `curtain_fabric`/`curtain_fabric_meters`, а сервер шлёт UUID в `curtain_fabric`, название в `curtain_fabric_name`, метраж в `curtain_meters`. Тип приведён к контракту `_get_designer_section`, экран читает правильные поля. (5) У склада и швеи скрыты «Создать КП», ₸, добавление замера, ⋮, редактирование/удаление заказа и кнопка смены статуса — бэкенд эти действия им и не даёт. Тесты: `test_measurement_role_flags.py` (8). Backend 123, mobile 58.
- **Удалены осиротевшие компоненты мобилки** (2026-07-18): `OrderMeasurements.tsx`, `OrderRoleActions.tsx`, `OrderSummaryCard.tsx` — ни один не импортировался после редизайна под Figma. Не косметика: именно неподключённый `OrderRoleActions` был причиной того, что мобилка вообще не умела менять статус, а `OrderMeasurements` содержал тот же баг с UUID вместо названия ткани. Восстанавливаются из git при необходимости.

- **Ролевой подстатус «Исполнение»** (2026-07-18): третья величина рядом со статусом заказа и складской шкалой — ответ на вопрос «этот заказ сейчас на мне». Правила (со слов владельца): швейный цех видит подстатус от момента, когда склад отметил материалы готовыми, и до момента, когда сшиты все изделия; установщик — от «все сшито» до загрузки АВР. Один заказ одновременно «в исполнении» для одной роли и нет для другой. Реализация: `atelier_erp/api/v1/substatus.py` — правила + `execution_substatus_annotations()` (`windows_total`/`windows_sewn`/`has_completion_act`), навешиваются в `OrderViewSet.get_queryset`, иначе расчёт даёт N+1 на списке. Поле `execution_substatus` в `OrderListSerializer` (SerializerMethodField, роль берётся из `context['request'].user`); есть фолбэк-путь по связям, если объект получен в обход аннотированного queryset. Подпись/цвет — в мобилке (`EXECUTION_SUBSTATUS_LABEL`), жёлтой строкой над номером заказа; статус справа остаётся. Тесты: `test_execution_substatus.py` (13, включая сквозной путь через `/api/v1/orders/`).
- **Фильтры и видимость по ролям** (2026-07-18): владелец/дизайнер — 4 группы (В работе / Ожидание / Завершён / Просрочен); склад — Закуп / Сборка / Готово (шкала `material_readiness`, пилюли переименованы с «Закупить/Нужно сделать/Сделано»); цех и установщик — только В работе / Просрочен (`EXECUTOR_FILTERS`), завершённые и ожидающие им не нужны. Ролевые срезы queryset на бэке **не расширялись** — это RBAC, менять без нужды не стал: цех по-прежнему видит только `in_production`, установщик — `ready`/`on_installation`/`waiting_final_payment`. В карточке окна добавлена строка «Количество: 1» — обозначение единицы (одно окно = одно изделие), отдельного поля у замера нет.

- **Экран установщика** (2026-07-18, migration 0027): `Measurement.installation_done` — третий независимый флаг по окну (склад собрал → цех сшил → монтаж повесил), `MeasurementInstallerFlagSerializer` + `Roles.INSTALLER` в `CanAccessMeasurement.CHECKBOX_ONLY`. `PhotoReport.measurement` (FK, nullable) — фото привязывается к окну, а не к заказу: `order_item` для этого не годится, позиции генерируются из КП и с окнами замера не связаны один к одному. Эндпоинты: `GET/POST /orders/{id}/photo-reports/` научились `?measurement=` и полю `measurement` (чужой замер → 400 `measurement_not_found`), добавлен `DELETE /orders/{id}/photo-reports/{photo_id}/` (мягкое удаление). Фото попадают в execution summary полем `photos` с `prefetch_related` (иначе N+1 по окнам). Мобилка: карточка установщика показывает только адрес и срок, секция называется «Изделия», кнопка «Загрузить АВР» (`expo-document-picker`, PDF/изображение → `completion-act` + `completion-act/upload-signed`) с именем файла, галочки по окнам, в карточке окна — «Прикреплённые фото» с добавлением (`expo-image-picker`) и удалением. Тесты: `test_photo_reports_per_window.py` (6). **Новая зависимость мобилки: `expo-document-picker` ~14.0.8** (входит в Expo Go, пересборка не нужна).
- **Правило загрузки фотоотчёта ослаблено осознанно** (2026-07-18): раньше фото пускались только при `handover_stage == done`, то есть требовалось сначала отметить установку завершённой и только потом приложить фото установки — обратный порядок, из-за которого экран установщика был неработоспособен. Добавлено условие: заказ в статусе `ready`/`on_installation`.
- **Битая картинка давала 500 вместо 400** (2026-07-18): валидация загружаемых изображений ловила `(UnidentifiedImageError, OSError)`, но Pillow отвечает на повреждённую структуру файла (например, PNG с неверной CRC чанка) исключением `SyntaxError` — оно улетало в 500. Добавлены `SyntaxError, ValueError` в обоих местах (`PhotoReportUploadSerializer`, `OrderCompletionActUploadSerializer`). Найдено случайно: тест собрал PNG вручную с битой CRC.

- **Единый источник подписей статусов** (2026-07-18): с пилота — в списке заказ «Просрочен», внутри «Новый»/«Ожидает финальной оплаты». Причина: список брал текст из `compute_ui_badge` (свои подписи «Новый», «Ожидание оплаты», «Ожидание материалов»), карточка — сырой `status_label`, а мобилка держала третью копию правил в `getOrderIndicator`. Раскладка групп вынесена из `filters.py` в новый `atelier_erp/api/v1/status_groups.py` (`ORDER_STATUS_GROUPS`, `GROUP_LABELS`, `get_status_group`, `get_status_group_label`); `filters.py` реэкспортирует для обратной совместимости. `compute_ui_badge` теперь отдаёт только групповую подпись (цвет по группе), в `OrderListSerializer` и в execution summary добавлены `status_group`/`status_group_label`, мобилка читает их и в списке, и в карточке. Наружу выходят ровно 4 подписи: В работе / Ожидание / Завершён / Просрочен — «Новый» больше не показывается (заказ в статусе `new` = «Ожидание»), «Ожидание материалов» ушло насовсем (это складская шкала Закуп/Сборка/Готово, отдельная величина). Обновлены 3 старых теста `UIBadgeTests`, закреплявших прежнее поведение. Тесты: `test_status_group_consistency.py` (5, включая ровно тот кейс с пилота).

- **Веб-КП: модалка вместо полноэкранной страницы** (2026-07-18): компонент `CreateKPModal` уже соответствовал макету (Предытог / Установка ₸ / Скидка % / ИТОГО / Предоплата % / Скачать КП), но открывался **только когда КП ещё нет** — при существующем КП карточка заказа вела на `/orders/[id]/quote` (старый 564-строчный билдер, совсем не как макет). Теперь обе кнопки («Создать КП» / «Открыть КП») открывают одну модалку. Второй баг: модалка строила строки из `order.items`, а они генерируются ИЗ утверждённого КП — у нового заказа их нет, поэтому показывалось «Нет позиций — сначала добавьте замер» даже при наличии замеров, и создать КП с веба было нельзя. Добавлен фолбэк на `order.measurements` с ручным вводом цены по окну (авторасчёт цены запаркован, как и в мобилке). Скидка теперь считается от «предытог + установка» — как в мобильном экране, иначе веб и телефон давали разное ИТОГО. Старая страница `/orders/[id]/quote` пока не удалена (доступна по прямой ссылке).
- **seed_demo переписан** (2026-07-18): раньше создавал только клиентов и «пустые» заказы — без замеров, КП, адресов и дат, из-за чего экраны выглядели незаполненными и проверять галочки/подстатусы было не на чем. Теперь 12 заказов по всем статусам (включая просроченные и отменённый), у каждого: адрес, дата замера, плановое завершение, 2–4 замера с обеими тканями и посчитанным метражом, КП с позициями, согласованные `total_amount`/`paid_amount`, операционные стадии под статус и флаги по окнам (`materials_ready`/`sewing_done`/`installation_done`) — часть окон намеренно не дошита, чтобы был виден подстатус «Исполнение». `--reset` чинён: `Quote.customer` стоит на `PROTECT`, поэтому КП удаляются до клиентов. Тесты: `test_seed_demo.py` (2).

- **Прод засеян демо-данными** (2026-07-18): запущено `seed_demo --reset --no-tenant` против прод-БД через публичный прокси Railway (`acela.proxy.rlwy.net:17423`, переменная `DATABASE_PUBLIC_URL` сервиса Postgres; `railway run` не годится — `postgres.railway.internal` резолвится только внутри сети). Итог в проде: 14 заказов по всем 8 статусам (4 просроченных), 37 замеров, 13 КП.
  - **Важная находка про multi-tenancy**: тенант в проде один (`sheber`), но `TenantMembership` есть **только у `admin`**. Все шесть пилотных аккаунтов `test_atelie_*` — без членства, значит их контекст тенанта `None`, и `TenantManager` отдаёт им `filter(tenant__isnull=True)`. Все существовавшие заказы тоже с `tenant=NULL`. Поэтому сев с привязкой к `sheber` сделал бы данные невидимыми под теми аккаунтами, с которых идёт проверка — засеяно с `tenant=NULL` (флаг `--no-tenant`), чтобы совпадало с текущим состоянием. **Тех-долг**: привести к согласованности — выдать пилотным пользователям `TenantMembership` в `sheber` и проставить `tenant` на существующих данных, иначе multi-tenancy на проде фактически не задействован.
  - Сидер доработан по ходу: `--tenant-slug` + автоопределение единственного тенанта, `--no-tenant`; `--reset` больше не удаляет демо-клиентов, на которых ссылаются заказы, созданные вручную через приложение (иначе `PROTECT` ронял команду), и клиенты создаются через `get_or_create` по телефону (он уникален, уцелевшие клиенты давали `duplicate key`). Обе ошибки ловились транзакцией — прод не был затронут.

- **Веб: номер заказа, дата, замеры в «Позициях»** (2026-07-18): (1) в списке заказов номер сокращён до «№933» — новый `frontend/src/lib/order-number.ts`; собственный номер ателье («ЗАКАЗ-А1») показывается как есть, иначе превратился бы в «№1». (2) Дата создания в списке была `year: "2-digit"` («18.07.26») → `numeric` («18.07.2026»). (3) В форме создания заказа первым полем добавлен **свой номер заказа** (пусто — присваивает нумератор); на бэке `order_number` принимается `OrderCreateSerializer` с проверкой уникальности. (4) Блок «Позиции» на карточке заказа показывал «Нет позиций» при наличии замеров, т.к. `OrderItem`ы генерируются ИЗ утверждённого КП — добавлен рендер окон из `order.measurements` с ценой из КП по паре комната+окно.
- **Латентный баг нумерации (найден и починен)** (2026-07-18): `OrderService._validate_order_number` требовал ровно `^О-\d{4}-\d{3}$`, а `next_number` форматирует значение как `{:03d}` — на **тысячном** заказе получается «О-2026-1000» (4 цифры), и создание заказов сломалось бы полностью. В проде на момент правки номера уже дошли до **933**, то есть до отказа оставалось ~67 заказов. Валидатор ослаблен до осмысленной проверки (непустая строка ≤50 символов без переводов строк); уникальность проверяется отдельно. Заодно это и разблокировало собственную нумерацию ателье.

- **Веб: разворот строки в «Позициях» + короткий номер везде** (2026-07-18): новый компонент `MeasurementRow` на карточке заказа — по клику строка раскрывается и показывает «Шторы: <ткань> (N м)», «Тюль: …», «Тип крепления», «Количество: 1» и комментарий, справа кнопки редактирования/удаления замера (удаление через `useDeleteMeasurement` + `refetchOrder`). Отдельный компонент от `ItemRow`: тот работает с `OrderItem`, а до утверждения КП позиций не существует. Итоговая строка блока переименована в «Предытог» (по макету) и считается по окнам с проставленной ценой. Короткий номер (`shortOrderNumber`) распространён на заголовок карточки заказа — было «Заказ №О-2026-933», стало «Заказ №933» — и на очереди работ (`workspace/page.tsx`, 2 места), иначе номера снова разъехались бы между экранами.

- **Двойной слэш в URL http-сервисов** (2026-07-19): изменение количества по окну падало с алертом «Not Found» — константа `BASE_ENDPOINT = "/v1/measurements/"` уже оканчивается слэшем, а вызовы клеили `${BASE}/${id}/` → `/v1/measurements//{id}/`, Django отдавал HTML-404. Тем же способом были сломаны получение замера по id, удаление замера, платёж по id и `addQuoteItem`. Починены все 5 мест. Модалка КП подставляла цены из существующего КП только в расчёт, но не в поля ввода — «Предытог» показывал сумму, строки стояли пустыми; теперь поля сеются в том же `useEffect`, что установка/скидка/предоплата. Тест `frontend/src/services/http/endpoints.test.ts` (16) проверяет инвариант «нет `//`, путь с `/v1/`» по 13 сервисным функциям; проверен мутацией — с возвращённой опечаткой падает 4 теста.
- **Утечка внутренних ошибок закрыта** (2026-07-19): 17 обработчиков `except Exception as e` в `api/v1/views.py` отдавали клиенту `str(e)` — в текст попадают имена ограничений БД и внутренние сообщения ORM. Добавлены `logger` + `_safe_error(exc, context)`: настоящая ошибка уходит в лог со стеком, клиент получает `UNEXPECTED_ERROR_MESSAGE`. Машиночитаемые `code` в ответах сохранены (фронт ориентируется на них). Осмысленные бизнес-ошибки (`OrderValidationError`, `InvalidOrderStatusTransition`) свой текст сохраняют — обобщается только необработанное. Тесты: `test_error_message_leakage.py` (4). Это был долг из аудита 2026-07-15.
- **Чистка репозитория** (2026-07-19): удалены 28 локальных веток (21 влита в `main`, 7 — снапшоты Cascade и допилотные состояния до чистки мёртвого кода), 3 worktree Windsurf в `~/.windsurf/worktrees`, 35 битых `tmp_obj_*` в `.git/objects`; `gc` → пак 6.2 МБ, `fsck` чистый. SHA удалённых веток сохранены в scratchpad. `.claude/settings.json` сокращён: убраны широкие маски `Bash(python -c ' *)` и `Bash(python -)`, разрешавшие произвольное исполнение Python без подтверждения; оставлены точечные и read-only git. Пустые каталоги `frontend/src/lib/api|http` (не игнорируемые, а просто пустые) удалены.

- **Замеры и справочники сужены по ролям** (2026-07-19): `MeasurementViewSet` не сужал выборку по роли — исполнитель не видел чужой заказ в списке, но мог прочитать и отметить галочку на его замере по прямому id; с автопродвижением статусов это меняло статус чужого заказа. Раскладка «роль → статусы» вынесена из `OrderViewSet.get_queryset` в новый `atelier_erp/api/v1/role_scope.py` (`ROLE_VISIBLE_STATUSES`, `scope_orders_for_role`, `filter_by_visible_orders`) и применяется к обоим — иначе у замеров была бы вторая копия правила. Заодно закрыты два справочника под голым `IsAuthenticated`: `/v1/tasks/` (лиды) и `/v1/customers/` (база клиентов с телефонами/почтой) — любая роль могла выгрузить их целиком, при этом ни один экран исполнителей туда не ходит; имя клиента по своему заказу исполнители по-прежнему видят через `OrderDetailSerializer`. Тесты: `test_measurement_role_scope.py` (6), `ExecutorCannotEnumerateDirectoriesTests` (4); оба набора проверены мутацией.

- **Три бага полного цикла заказа, найдены сквозным прогоном new → completed через реальные API** (2026-07-20): владелец попросил провести заказ по всем этапам вручную — тут и вскрылось.
  1. **`order.total_amount` никогда не синхронизировался с суммой КП.** `OrderItemGenerationService.generate_order_items_from_quote` создавал `OrderItem`'ы, но не трогал `order.total_amount` — заказ навсегда оставался «должен 0». Из-за этого `change_handover_stage` видел `balance_due=0` и не переводил заказ в `waiting_final_payment`, а прямой переход `on_installation → completed` запрещён FSM — **заказ навсегда зависал в `on_installation`**, независимо от реальной оплаты. Фикс: после генерации позиций `order.total_amount = source_quote.total` (не сумма позиций — она не включает `installation_cost`/`delivery_cost`/скидку).
  2. **`POST /api/v1/payments/` не обновлял `order.paid_amount`.** `PaymentViewSet` использовал дефолтный `ModelViewSet.create`, который просто пишет строку `Payment`, минуя `PaymentService.record_payment` (единственное место, где `paid_amount` растёт и запускается авто-завершение). Это тот самый эндпоинт, которым пользуется веб (`createPayment`). Фикс: `PaymentViewSet.create` переписан на вызов `PaymentService.record_payment`.
  3. **`ready → on_installation` автоматизирован** по прямому запросу: ручной шаг владельца был формальностью — монтажник и так уже мог действовать по заказу в статусе `ready` (фотоотчёт/АВР/handover разрешены начиная с `ready`). Теперь `production_stage=done` сразу цепочкой ведёт `→ ready → on_installation` в `OrderExecutionService.change_production_stage`.
  4. **Смежный долг тоже закрыт в этом же заходе:** `manage_item` (PATCH/DELETE позиции) пересчитывал `order.total_amount` только суммой позиций, без `installation_cost`/`delivery_cost`/скидки КП — редактирование позиции после генерации откатило бы сумму заказа ниже реальной суммы КП. Формула вынесена в один метод `OrderItemGenerationService.recalculate_order_total(order)` (сумма позиций + extras из `_get_source_quote`), используется и после генерации, и в `manage_item` — больше нет двух копий одной формулы.
  Тесты: `test_status_automation.py` (+2: `total_amount` синхронизация, `ready→on_installation` каскад), новый `test_payment_api.py` (3). Backend 201, frontend 47, mobile 58 — все зелёные.

- **Security-аудит архитектуры (полный, B1–J1)** (2026-07-20): `docs/SECURITY_ARCHITECTURE_AUDIT.md` + результаты по этапам в `docs/security-audit/`. 25 находок с приоритетом, roadmap в `J1-consolidated-overview.md`. Топ-3 High: throttle-кеш не общий между воркерами (LocMemCache), единственный superuser без MFA с автосбросом пароля на каждом деплое, публичный `DATABASE_PUBLIC_URL` в обход приложения. A1 (scope) остался ручной задачей владельца — не блокировал остальное.
- **Быстрые фиксы из аудита** (2026-07-20): 4 из ~30-минутных пунктов roadmap закрыты сразу.
  1. **Throttle-кеш стал общим между gunicorn-воркерами** — добавлен `CACHES` (`FileBasedCache`, `settings.py`), раньше был неявный `LocMemCache` (память процесса), из-за чего лимит логина `5/min` по факту допускал ~15/мин при 3 воркерах. Тестовый профиль (`settings_test.py`) держит `LocMemCache` явно — файловый кеш персистентен между прогонами тестов и копил бы старые счётчики throttle.
  2. **Неудачные попытки логина теперь логируются** — `ThrottledTokenObtainPairView.post` (`auth_views.py`) ловит исключение от SimpleJWT (`AuthenticationFailed` вылетает ИЗ `super().post()`, просто сравнить `response.status_code` после вызова недостаточно — нужно поймать и провести через `self.handle_exception`), пишет `logger.warning` с username+IP, не паролем.
  3. **`ensure_superuser` больше не откатывает вручную изменённый пароль при каждом деплое.** Раньше `set_password` вызывался безусловно на каждом вызове команды (она в `CMD` контейнера, значит на каждом redeploy) — смена пароля через `/admin/` молча отменялась следующим деплоем. Теперь при существующем пользователе трогаются только `is_staff`/`is_superuser` (могли случайно снять), пароль — только с явным `--force-password`.
  4. **Простой CI** (`.github/workflows/ci.yml`) — раньше не было вообще никакого автозапуска: backend/frontend/mobile тесты (уже зелёные, 210/47/58) + `pip-audit`/`npm audit` (warn-only) + `gitleaks` на секреты, на каждый push/PR в `main`.
  Тесты: `test_ensure_superuser.py` (новый, 4), `test_p1_security_numbering.py` (+2: логирование неудачного/успешного логина). Backend 210, frontend 47 — зелёные.
  **Не тронуто из High/roadmap** (требует решения владельца или доступа вне кода): `DATABASE_PUBLIC_URL` (нужно смотреть план Railway), JWT в `localStorage` на вебе (нетривиальная правка — httpOnly-cookie), бэкапы БД (не код, консоль Railway).

- **Экран «Материалы»: призрачная строка-дубликат без «⋮»** (2026-07-20): владелец решил, что склад должен полностью управлять и тканями — оказалось, что он уже может. Корень «бага»: `InventoryItem` категории «ткань»/«тюль» зеркалится в `Fabric` сервисом `sync_fabric_from_inventory_item` (нужно для выпадашки тканей на замере — `Measurement` ссылается на `Fabric`, не на `InventoryItem`). Экран «Материалы» показывал ОБЕ строки без дедупликации: редактируемую (`InventoryItem`, есть «⋮») и её зеркало (`Fabric`, без «⋮», т.к. `/v1/inventory/` read-only) — выглядело так, будто у одного и того же материала действия то есть, то нет. Фикс: `materials/page.tsx` скрывает `Fabric`-строку, если есть `InventoryItem` той же категории с тем же именем (case-insensitive). Полноценного отдельного write-API для `Fabric` не делали и не нужно — `InventoryItem` уже покрывает все категории (ткань/тюль/карниз/фурнитура/прочее) с полным CRUD для склада (`IsWarehouseOrOwner`).
  Заодно почищена прод-БД: удалена ранее осиротевшая `Fabric`-запись `324234` (осталась от прямого `.delete()` мимо API при чистке демо-данных этой же сессией — обошёл `perform_destroy`/sync, поэтому зеркало не удалилось вместе с оригиналом).
  **Найден, но не чинили (не в этом заходе):** `_sanitize_hanger_number` (`inventory_fabric_sync.py`) при полностью нелатинском SKU (например, кириллица) падает на fallback `'MAT'` — если завести два разных тканевых `InventoryItem` с кириллическими артикулами, оба смэппятся на один `hanger_number='MAT'` и их `Fabric`-зеркала будут перезаписывать друг друга через `update_or_create`. Нужен уникальный fallback (например, короткий хэш/суффикс id) вместо константы.

- **Веб: форма «Создание заказа» — 3 правки по клиенту** (2026-07-20): (1) убрана зелёная подпись «✓ Имя» под полем клиента — дублировала то, что и так видно в самом поле поиска. (2) **Реальный баг**: `CustomerSearch` объявлял проп `value` в типе, но никогда не читал его — после инлайн-создания нового клиента (`InlineNewCustomer`) поле поиска оставалось пустым, хотя клиент был выбран (`customerId`/`customerName` в родителе обновлялись верно) — выглядело как «добавляет, но не выбирает». Теперь `value` (имя клиента) синкается в локальный `query` через `useEffect`. (3) Иконка кнопки «новый клиент» заменена с `User` на `UserPlus` (визуально понятнее, что это «добавить»). (4) В `InlineNewCustomer` добавлено необязательное поле «E-mail» (модель `Customer.email` уже поддерживало, в форме не было).
- **`_sanitize_hanger_number`: коллизия кириллических артикулов** (2026-07-20): при полностью нелатинском SKU (кириллица) после чистки regex давал пустую строку → фолбэк на константу `'MAT'`. Два разных тканевых `InventoryItem` с кириллическими артикулами схлопывались в один `hanger_number='MAT'`, и `update_or_create()` в `sync_fabric_from_inventory_item` затирал `Fabric`-зеркало одного материала другим. Фолбэк заменён на `ITM-{префикс id позиции}` — уникален и стабилен между повторными синками одной и той же позиции. Тест: `test_inventory_fabric_sync.py` (+1). Backend 211, frontend 47.

- **Веб: разводка ролей на 5 экранах по скринам владельца** (2026-07-20): (1) Дашборд временно скрыт для ВСЕХ ролей, включая владельца (`ROLE_LANDING` в `dashboard/page.tsx` дополнен `owner: "/orders"`) — требует переработки, страница не удалена, просто недостижима из обычной навигации. (2) `/orders` (список заказов): убрана стрелка «Назад» на дашборд (её и вело на скрытый теперь экран); добавлена кнопка «Материалы» рядом с «Клиенты» — видна Owner и Designer. (3) Экран «Материалы» (`work/warehouse/materials/page.tsx`) стал ролезависимым: `canEdit = owner || warehouse` — Designer видит список тканей/фурнитуры (нужно для КП), но без кнопки «Добавить позицию», без клика по строке и без меню «⋮» (полностью read-only); кнопка «Назад» теперь контекстная (`warehouse` → `/work/warehouse`, остальные → `/orders`, а не жёстко на склад). (4) Карточка заказа: `MyTaskCard` («МОЯ ЗАДАЧА») убрана и у Designer, и у швейного цеха (`role === "installation"` — единственная роль, где карточка осталась) — по прямому решению владельца оба экрана сведены к одной карточке-чеклисту, как у склада. Кнопка «Удалить заказ» дана Designer (была только Owner) — **вместе с бэкендом**: `OrderViewSet.destroy` теперь `IsOwnerOrDesigner` вместо `IsManagerOrAdmin` (безвозвратное удаление, не soft — решение подтверждено владельцем явно, `action` `cancel` остался Owner-only). (5) Экран швейного цеха (`work/production/page.tsx`): заголовок «Пошив» → «Заказы»; статусы пересобраны — «Собирается» (жёлтый, было «Не начато»/красный), «Пошив» (зелёный, было жёлтый), «Готово» (серый, было зелёный) — по прямому запросу владельца, семантика «жёлтый=ожидание, зелёный=активная работа, серый=завершено», согласовано с остальным приложением.
  Тесты: `test_v1_features_2026.py` (designer теперь МОЖЕТ удалять заказ, было — не может). Backend 211, frontend 47, tsc чист.

- **Ролевая видимость Склад/Швея/Установщик уравнена на «В работе + Просрочен»** (2026-07-20): изначально расширили только установщика (весь конвейер), но по уточнению владельца свели все три исполнительские роли к одному правилу — группа «В работе» (`in_work`/`in_production`/`ready`/`on_installation`) плюс любой просроченный заказ независимо от статуса. Реализовано как общий `_active_or_overdue_q()` в `atelier_erp/api/v1/role_scope.py` вместо статичных списков статусов на каждую роль (`ROLE_VISIBLE_STATUSES` теперь пуст — остался только как задел на будущее). Заодно на вебе: колонка «Статус» у установщика перестала показывать «—» (раньше брала `handover_stage`, теперь общий `getCoarseStatus`); убран дублирующий «Удалить позицию» в форме материала (уже есть через «⋮»); блок «История» убран с карточки заказа у швеи и установщика (не нужен). Тесты: backend 221 (+7 на видимость трёх ролей), frontend 47, tsc чист.
- **Замер: «Крепление» и «Фурнитура» из склада вместо текстового mounting_type** (2026-07-20): поле 7 формы замера «Тип крепления» (текстовый выбор из `MOUNTING_OPTIONS` — карниз/рулонная штора/жалюзи/…) заменено на «Крепление» — конкретную позицию склада категории «Карниз» (`InventoryItem`) с количеством в единице измерения самой позиции (метры/шт). Добавлено новое поле 8 «Фурнитура» — позиция категории «Фурнитура» + количество в штуках. Модель: `Measurement.cornice_item`/`cornice_quantity`/`hardware_item`/`hardware_quantity` (migration 0029), старое `mounting_type` оставлено как legacy-поле (не пишется новой формой, старые записи не тронуты). `MeasurementWriteSerializer` валидирует категорию каждой позиции (`cornice_item` обязан быть `category=cornice`, `hardware_item` — `category=accessory`). Компонент `create-measurement-modal.tsx` — общий для нового и легаси-экрана `measurements/page.tsx`, поэтому фикс подтянулся в обоих местах одним изменением. Отображение (карточка заказа, все 3 места: общая позиция + складская/швейная секции) показывает «Крепление: <название> (<кол-во> <ед>)» и «Фурнитура: …», если заполнено. Мобилка не тронута (мобильная форма замера продолжает писать `mounting_type` как раньше — известный пробел, см. follow-up). Тесты: `test_measurement_cornice_hardware.py` (6). Backend 221, frontend 47, tsc чист.

- **«Позиции» после утверждения КП: редактирование ведёт в замер, не в отдельную форму позиции** (2026-07-20): после утверждения КП блок «Позиции» показывает `OrderItem` (снимок для биллинга — цена/количество), у которого была своя урезанная форма редактирования (`EditItemModal`: «Тип пошива (ткань)»/«Тюль / заметки»/«Тип крепления (складок)») — физически не знавшая про ткань/крепление/фурнитуру, потому что таких полей у `OrderItem` просто нет. Из-за этого владелец увидел мусор вида «Тюль: спальня / 1 / standard» (эхо из `item.notes`) и решил, что это регрессия от правок этой же сессии — на деле это два разных, давно существующих экрана (замер до КП / позиция после КП), не связанных с фичей «Крепление/Фурнитура». По выбору владельца — унифицировано: `ItemRow` находит соответствующий `Measurement` по совпадению комната+окно и кнопка «✎» открывает ту же форму замера (`CreateMeasurementModal`, тот же инстанс на странице, что и до КП), а не `EditItemModal`. Отображение позиции (Шторы/Тюль/Крепление/Фурнитура) тоже переведено на данные замера. `EditItemModal` оставлен как фолбэк для позиций без окна (услуги/установка без room_name+window_name). Осознанный побочный эффект: `unit_price`/`quantity` товара-ткани в позиции больше не редактируются отдельно от замера этой формой (количество экземпляров окна по-прежнему меняется степпером в самой строке).

- **Замер: «Ткань штор»/«Ткань тюля» показывали общий список + осиротевший дубликат** (2026-07-20): `Fabric` не различал ткань/тюль (нет поля категории) — обе выпадашки замера показывали ВЕСЬ каталог целиком, плюс в списке светился «призрачный» дубль («йцуйцу (MAT)» рядом с «йцуйцу (ITM-…)») — артефакт старого бага с коллизией кириллических SKU (см. запись 149), который уже чинили для экрана «Материалы», но не для этой выпадашки. Корень дубликата глубже: `sync_fabric_from_inventory_item` матчил `Fabric` по `(tenant, hanger_number)` — при смене sku пересчитанный `hanger_number` переставал совпадать со старым, и `update_or_create()` заводил ВТОРУЮ запись вместо обновления первой, оставляя первую осиротевшей. Добавлены `Fabric.category` (`fabric`/`tulle`, синкается из `InventoryItem.category`) и `Fabric.source_item` (прямая `OneToOneField` на `InventoryItem` — синк матчит по ней, а не по `hanger_number`, так что смена sku больше не плодит дубли). Migration 0030 бэкофиллит `category`/`source_item` для уже существующих записей тем же доматчиванием по старому ключу; записи без живого `InventoryItem`-источника (как раз тот осиротевший дубль) остаются с пустой `category` — и поэтому перестают попадать в отфильтрованные по категории выпадашки, не будучи удалены руками из прод-БД. Фронт: `useFabrics`/`fetchFabrics` получили параметр `category`, `create-measurement-modal.tsx` делает два раздельных запроса (`category=fabric` для поля 5, `category=tulle` для поля 6) вместо одного общего списка. `InventoryAvailabilityViewSet.filterset_fields` дополнен `category`. Тесты: `test_inventory_fabric_sync.py` (+3: категория проставляется корректно, список фильтруется по категории, смена sku не плодит дубликат). Backend 224, frontend 47, tsc чист. Миграция накатится на проде автоматически при следующем деплое (`CMD` контейнера гоняет `migrate --noinput`) — отдельного ручного вмешательства в прод-БД не потребовалось.

### ⚠️ Не запушено
На 2026-07-19 всё запушено, `origin/main` = `main`. В репозитории одна ветка — `main`.


## Vercel (фронтенд деплой) — состояние на 2026-06-09
- Проект: https://vercel.com/bohemetextile8-2199s-projects/atelier-erp
- GitHub: xoxmach7/atelier_erp, ветка main, Root Directory: frontend
- **Проблема была**: Framework Preset = "Other" → Vercel не создавал serverless functions
- **Фикс**: изменить Framework Preset на "Next.js" в Settings → Build & Development Settings → сохранить → редеплой
- Build command: `next build --webpack` (явно отключён Turbopack, commit 89cde7e)
- NEXT_PUBLIC_API_BASE_URL выставлен в Vercel env vars
- ✅ РАБОТАЕТ: https://atelier-erp.vercel.app — логин проходит
- Домен добавлен в CORS_ALLOWED_ORIGINS и CSRF_TRUSTED_ORIGINS в Railway Variables

## VirtioFS — важно для файловых правок
- Edit/Write инструменты ПИШУТ в Windows-папку корректно (на машине пользователя файл верный сразу).
- НО: sandbox (bash/python) читает host-правки с задержкой и иногда «рваными» страницами (torn reads) — `import`/`tsc`/`makemigrations` могут видеть устаревший/обрезанный файл. cp-roundtrip `mount→/tmp→mount` ОПАСЕН (рваное чтение может побить файл).
- Надёжный приём для проверки в sandbox: писать файл со стороны sandbox (heredoc/`git show HEAD:path` → правка в /tmp → один `cp /tmp→mount`), затем гонять tsc/pytest. Целостность сверять через `git diff --numstat` (не должно быть лишних удалений) и AST/py_compile.
- Git операции — только из терминала пользователя, не из sandbox.

## Тесты (зелёные MVP, 41 штука)
```
python manage.py test --settings=atelier_erp.settings_test \
  atelier_erp.tests.test_order_lifecycle_v1_api \
  atelier_erp.tests.test_role_access \
  atelier_erp.tests.test_p1_security_numbering
```
pytest/pytest-django закреплены в requirements.txt — прогон подтверждён 2026-07-11 (41 тест, зелёные).

## Что сделано в P0/P1
- Единый реестр ролей (`atelier_erp/roles.py`)
- RBAC: срез заказов по ролям, default deny
- JWT blacklist + logout + rate throttle (5/min на логин)
- Атомарная нумерация (`NumberSequence`)
- Railway deploy (Gunicorn + WhiteNoise)
- Чистка ~4k строк мёртвого кода

## Цветовая система (frontend globals.css)
| Переменная | Hex | Назначение |
|-----------|-----|-----------|
| --a | #0EA5E9 | Primary (sky-500) |
| --ad | #0284C7 | Primary dark |
| --al | #E0F2FE | Primary light |
| --bg | #F0F4F8 | Фон страницы |
| --t1 | #0F172A | Текст основной |
| --t2 | #475569 | Текст второстепенный |
| --t3 | #94A3B8 | Текст серый |
| --ok | #16A34A | Успех |
| --warn | #D97706 | Предупреждение |
| --err | #DC2626 | Ошибка |

## Архитектурные дыры — актуальный статус (2026-07-11)

См. полную историю multi-tenancy и security-фиксов в разделе «Статус задач» выше.

### 🔲 Остаётся
### P4-OPS: Нет email-уведомлений
EMAIL_BACKEND не настроен. Сотрудники не получают уведомлений о смене статуса.
Решение: SendGrid или Gmail SMTP.
СДЕЛАТЬ ПОСЛЕ ПЕРВОГО КЛИЕНТА.

### P5: Ручной онбординг нового ателье
Каждый новый клиент — `manage.py create_tenant --name=... --slug=...` в Railway Console.
Решение: wizard онбординга + self-service регистрация.
ПОСЛЕ 5+ КЛИЕНТОВ.

### P5: Нет биллинг-модели для SaaS
Нет учёта подписок от ателье. Пока ручное управление.
ПОСЛЕ 5+ КЛИЕНТОВ.